services:
  web:
    build: .
    ports:
      - "80:80"
    environment:
      - SECRET_KEY_BASE=${SECRET_KEY_BASE}
    volumes:
      - storage:/rails/storage
    restart: unless-stopped

volumes:
  storage:
